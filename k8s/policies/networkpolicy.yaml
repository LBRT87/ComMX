apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: commx-database-isolation
  namespace: commx
spec:
  podSelector:
    matchExpressions:
      - key: app.kubernetes.io/name
        operator: In
        values: [postgres, redis]
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: backend
      ports:
        - protocol: TCP
          port: 5432
        - protocol: TCP
          port: 6379
